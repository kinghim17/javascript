//json stands for the javascript object notation
const users={
    name:"himanshu",
    age:24,
    sex:"male"
}
console.log(users["sex"]);
const convert=JSON.stringify(users);
console.log(users.name);

// we convert json to strings through stringfy and strings to json by PARSE 
