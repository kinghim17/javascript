
function resi(str) {
   console.log("String length:", str.length);
 }

 resi("himanshu mohapatra");
 // Comment: Many other string APIs exist, such as slice, split, trim, toUpperCase, toLowerCase, etc.
 
 const array = [1, 2, 3, 4, 5];
 array.pop();
 array.push(1);
 console.log("Array elements:");
 for (let i = 0; i < array.length; i++) {
   console.log(array[i]);
 }
 

 